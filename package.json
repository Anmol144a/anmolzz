// package.json  (place it in the repo root)
{
  "name": "anmol-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site – nothing to build' && mkdir -p public && cp index.html public/ && cp -r src public/ && cp -r public/assets public/"
  }
}
