{
  "name": "anmol-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public/assets && cp index.html public/ && cp -r src public/ && cp -r public/assets public/"
  }
}
